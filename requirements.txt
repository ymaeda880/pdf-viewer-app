# requirements.txt — PDF Viewer + OCR

# --- Streamlit 本体 ---
streamlit>=1.33.0
# st.pdf 用の PDF コンポーネントを有効化
streamlit[pdf]

# --- PDF 閲覧・処理 ---
PyMuPDF>=1.24.10              # fitz: PDFテキスト/サムネ抽出
streamlit-pdf-viewer>=0.0.12  # PDFビューワー埋め込み

# --- OCR 処理 ---
ocrmypdf>=16.0.0              # OCRmyPDF (Python API も利用可能)

# --- Excel 読み書き ---
pandas>=2.0.0
openpyxl>=3.1.0         # pandasで.xlsxを読む用
XlsxWriter>=3.1.0       # pandasのExcel書き出し(engine="xlsxwriter")
xlrd==1.2.0   # .xls 読み取り用（1.2.0で固定推奨）

